# Jour 2 — API REST avec Spring Boot

## Objectifs
- Manipuler les méthodes GET, POST, PUT, DELETE
- Gérer des données via une liste en mémoire
- Utiliser `@RequestParam`, `@PathVariable`, `@RequestBody`

## Commandes utiles
```bash
./mvnw spring-boot:run
```